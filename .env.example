# Booking WhatsApp number — digits only, full international format, no "+".
# Used for every WhatsApp CTA on the site and the schema.org telephone fields.
# Set this in Vercel → Project → Settings → Environment Variables.
# NEXT_PUBLIC_* values are inlined at build time, so redeploy after changing it.
NEXT_PUBLIC_WHATSAPP_NUMBER=447348644054

SUPABASE_SECRET_KEY=
